/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'chat-web-mhung.s3.ap-southeast-2.amazonaws.com',
      'lh3.googleusercontent.com',
    ],
  },
};

export default nextConfig;
